# CoPiano v3 — Python Dependencies
# Tested on: Python 3.10+, macOS M1/M2/M4 + Linux (Ubuntu 22.04)
# 分层安装:core (必须) / audio (可选) / llm (可选) / dev (可选)

# ========== Core (必须) ==========
# 数据处理 + MIDI 解析 + 乐理分析
numpy>=1.24,<3.0
scipy>=1.10
matplotlib>=3.7,<4.0
pretty_midi>=0.2.9
mido>=1.3
music21>=8.3
scikit-learn>=1.3

# 网络 + 异步
edge-tts>=6.1.9
aiohttp>=3.9

# ========== Audio (可选, real piano) ==========
# sounddevice + librosa 用于真实钢琴录音分析
sounddevice>=0.4.6
librosa>=0.10
soundfile>=0.12
pydub>=0.25

# ========== LLM (可选, GPU 推荐) ==========
# 模型加载 + 推理 (本地 Qwen 7B 推荐)
torch>=2.0,<3.0
transformers>=4.30
modelscope>=1.10
accelerate>=0.20

# ASR (语音识别, faster-whisper)
faster-whisper>=0.10
ctranslate2>=3.16

# ========== Dev (可选, 开发/测试) ==========
# 测试 + 类型检查
pytest>=7.4
pytest-cov>=4.1
mypy>=1.5
black>=23.7
flake8>=6.0

# Jupyter (可选, 数据探索)
jupyter>=1.0
ipykernel>=6.25

# ========== Optional / 已内置 ==========
# tonnetz_kg.py / llm_daemon.py / etc. 是 CoPiano 自带, 不需安装
# music21 需要 lilypond / MuseScore (可选, 用于乐谱渲染)
# - macOS: brew install lilypond
# - Ubuntu: apt-get install lilypond

# ========== 平台特定 ==========
# macOS (Apple Silicon):
#   pip install --no-cache-dir torch torchaudio
# Linux + CUDA:
#   pip install torch==2.0.1+cu118 --index-url https://download.pytorch.org/whl/cu118
#   pip install transformers accelerate
