# CoPiano v4 Backend — Python Dependencies
# ──────────────────────────────────────────────
# 安装: pip install -r requirements.txt
# 锁定: pip freeze > requirements.lock.txt
# ──────────────────────────────────────────────

# Core — FastAPI 框架
fastapi==0.140.13
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32

# Database — PostgreSQL + SQLAlchemy 2.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
asyncpg==0.31.0
aiosqlite==0.22.1
alembic==1.18.5

# Cache + Queue — Redis
redis==5.1.1
celery==5.6.3

# Auth — JWT + OAuth
pyjwt==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.5.0
authlib==1.7.2
email-validator==2.3.0

# HTTP Clients
httpx==0.28.1
aiohttp==3.14.3

# Storage — S3 / MinIO
boto3==1.43.58

# MIDI + Audio (复用 v3.0 模块)
mido==1.3.3
pretty-midi==0.2.11.post0
music21==9.1.0
numpy==1.26.4

# LLM — Qwen (本地 GPU) + OpenAI
openai==1.51.0
transformers==4.45.2
torch==2.13.0
accelerate==0.34.2

# Monitoring + Logging
prometheus-client==0.26.0
sentry-sdk[fastapi]==2.66.1
structlog==24.4.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.28.1
moto[s3]==5.2.2
boto3==1.43.58

# Development
black==24.10.0
ruff==0.16.0
ipython==8.28.0

# 2026-07-28 部署发现缺失,补上
slowapi==0.1.10
opencv-python-headless==4.10.0.84
mediapipe==0.10.14
